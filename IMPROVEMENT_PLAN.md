# Router R106 插件改进计划

## 1. 问题背景

当前 `router_r106` 自定义集成存在以下问题：

*   **加载失败**：如果 Home Assistant 启动或加载插件时，目标路由器设备离线或网络不可达，插件的初始化过程 (`setup_platform`) 会因为无法登录路由器而失败，导致整个插件加载失败，并在日志中记录 `Failed to load integration: router_r106`。
*   **无法自动恢复**：即使插件在运行时因路由器离线而断开连接，当路由器重新上线后，插件可能无法自动恢复正常工作，需要用户手动重启 Home Assistant。

## 2. 核心改进计划

为了解决上述问题，计划实施以下两方面的核心改进：

### 2.1. 改进插件加载逻辑 (`setup_platform`)

*   **目标**：确保即使在 Home Assistant 启动或加载插件时路由器暂时不可用，插件本身也能够成功加载，而不是直接报错失败。
*   **方法**：
    1.  修改 `setup_platform` 函数。
    2.  在尝试初始登录路由器时，如果失败：
        *   不再立即让整个插件加载失败 (`return False`)。
        *   允许插件加载完成 (`return True`)。
        *   相关的传感器实体暂时设置为“不可用” (unavailable) 状态。
        *   记录一条明确的日志，说明初始连接失败，并将依赖后续的重连机制。

### 2.2. 增强运行时的自动重连机制

*   **目标**：一旦插件成功加载（即使初始连接失败），它需要有一个健壮的后台机制，能够持续、可靠地尝试连接到路由器，直到成功为止，并在连接断开后自动恢复。
*   **方法**：
    1.  在 `RouterAPI` 类中实现或增强后台重连逻辑。
    2.  当 `get_status` (或其他数据获取方法) 因为未登录或连接失败而无法获取数据时，触发一个更积极的登录/重连尝试循环。
    3.  此循环应包含：
        *   多次重试。
        *   每次重试之间有逐步增加的延迟（退避策略）。
        *   设定合理的总体超时或最大尝试次数，避免无限循环阻塞。
    4.  重连成功后，恢复传感器实体的正常更新。
    5.  在重连的各个阶段（尝试、成功、持续失败）添加详细的日志记录。

## 3. 预期目标

*   **提高插件加载的健壮性**：避免因启动时路由器临时不可用而导致插件彻底加载失败。
*   **实现真正的自动恢复**：在路由器断线重连后，插件能够自动、可靠地重新连接并恢复正常工作，最大程度减少用户手动干预（如重启 Home Assistant）的需求。

## 4. 流程示意图

```mermaid
graph TD
    A[HA 尝试加载 router_r106 插件] --> B{setup_platform 执行};
    B --> C[尝试初始登录路由器];
    C -- 成功 --> D[创建传感器实体并正常运行];
    C -- 失败 --> E[插件加载仍成功，记录初始连接失败日志];
    E --> F[传感器实体暂时为 unavailable];
    F --> G[后台启动持续重连机制];
    D --> H{运行时路由器是否离线?};
    H -- 是 --> I[连接丢失，标记未登录];
    I --> G;
    H -- 否 --> D;
    G -- 尝试重连 --> J{重连是否成功?};
    J -- 是 --> D;
    J -- 否 --> K{达到最大重试/超时?};
    K -- 否 --> L[等待后再次尝试];
    L --> G;
    K -- 是 --> M[保持 unavailable，记录错误];
    M --> G;